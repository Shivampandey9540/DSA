//how much Time it will take
function totalTimeCal(arr, eatingHour) {
  let total = 0;
  for (let pile = 0; pile < arr.length; pile++) {

    total = total + Math.ceil(arr[pile] / eatingHour);
    // console.log(total);
  }
  return total;
}



function kookuEatingBanaan(arr){
    let 
}

