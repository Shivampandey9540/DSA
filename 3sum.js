let arr = [-1, 0, 1, 2, -1, -4];

function findThreeSum() {
  let set = {};

  for (let fist_itra = 0; fist_itra <= arr.length - 1; fist_itra++) {
    for (
      let seco_itr = fist_itra + 1;
      seco_itr <= arr.length - 1;
      seco_itr++
    ) {

        
    }
  }
  //   console.log(set);
}

console.log(findThreeSum(arr));
